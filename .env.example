# Ключ Anthropic. Используется ТОЛЬКО в server route (src/app/api/**),
# в клиентский бандл не попадает и префикса NEXT_PUBLIC_ не получает.
# Реальное значение кладём в .env.local, который не коммитится.
ANTHROPIC_API_KEY=
